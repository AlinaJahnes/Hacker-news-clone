<h2>Hacker News</h2>

<ol>
<% @articles.each do |article| %>
  <div><li><%= link_to "▲", like_count_path(article), id: "like_count", method: :post %>
    <span id="span<%=article.id%>">votes: <%= article.like_count %></span>
    <%= link_to article.title, article_path(article) %>  written by <%= article.user.username%></li></div>
<% end %>
</ol>
