<h2>Hacker News</h2>

<ol>
<% @articles.each do |article| %>
  <div>
    <li>
      <%= form_for @vote, url: votes_path(article), html: {class: 'vote_count'} do |f| %>
      <%= f.hidden_field :user_id, value: current_user.id %>
      <%= f.hidden_field :article_id, value: article.id %>
      <%= f.submit "↑" %>
  <% end %>
      <span id="count<%= article.id%>">votes: <%= article.votes.count %></span>
  		<%= link_to article.title, article_path(article) %><div>  written by <%= article.user.username%><br><%= article.comments.count%> Comments</div>
    </li>
  </div>
<% end %>
</ol>

